services:
  - type: web
    name: magicpin-ai-bot
    env: docker
    plan: free
    dockerCommand: "uvicorn bot.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: XAI_API_KEY
        sync: false # Must be set manually in Render dashboard
